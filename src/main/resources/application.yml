application:
  name: @name@
  version: @version@

spring:
  output:
    ansi:
      enabled: ALWAYS
  application:
    name: ${application.name}
  data:
    mongodb:
      database: frota_znh
      host: m2m-mongo-hml

grpc:
  port: 6565

logging:
  path: log
  maxHistory: 30
  fileSize: 1MB

hazelcast:
  cache-instances:
    - localhost:5701

redis:
  host: localhost
  porta: 6379
